# Data Model: Password Toggle Icon Correction

No entities, no state changes. The existing `visible: boolean` from Feature 010's `PasswordInput` is reused as-is — only which icon renders for each of its two values changes.
